# LaTeX Compile Issue Log

Record compile, formatting, package, or content-parity issues here.

| Date | Task | File / Area | Issue | Status | Resolution |
|---|---|---|---|---|---|
| 2026-06-12 | T-092 | Bootstrap | Initial skeleton created. | Closed | `pdflatex` produced `main.pdf`; see T-092 progress notes. |
| 2026-06-12 | T-092 | Tooling | `latexmk` cannot run because MiKTeX cannot find the required Perl script engine. | Resolved (Claude) | Root cause: latexmk is a Perl script and MiKTeX bundles no Perl. Perl already exists on this machine (Git for Windows, `D:\Git\Git\usr\bin\perl.exe`). `scripts/build.ps1` now auto-detects Perl (Strawberry, then Git for Windows) and prepends it to the session PATH before calling latexmk. Verified: `latexmk -pdf` compiled the stub to 22 pages and ran biber successfully. No global PATH change; Overleaf unaffected. |
| 2026-06-12 | T-092 | Bibliography | Stub project has an intentionally empty bibliography because T-096 has not migrated `references.bib`. | Resolved (Claude) | Ported the real `references.bib` (76 entries) into `docs/thesis-latex/bibliography/`. `latexmk -xelatex` now resolves all cited keys: 0 undefined; Chapter 1 renders proper APA in-text citations. Bold raw-key rendering was the biblatex "undefined citation" fallback from the empty placeholder, not a prose error. |
| 2026-06-12 | Citations | Chapter PDFs showing bold raw keys (e.g. `istqb_ctfl_syllabus_2024`) | User flagged citations as non-APA. | Resolved (Claude) | Confirmed prose already uses correct `\autocite{}` (APA via `style=apa`); the bold-underscore key was a placeholder-bib symptom. Fixed by the bibliography port above. Documented the authoritative APA in-text convention in `latex-migration-spec.md` Section 5a and `citation-register.md`. |
| 2026-06-12 | Citations | Self-citations to the author's own GitHub repo (`project_*_2026`, e.g. `(Le, 2026a, 2026b)`) | User: citing one's own unpublished repo in-text is not appropriate academic practice. | Resolved (Claude) | Removed all 19 `project_*_2026` keys thesis-wide (Markdown Ch1/3/4/6 + definitions + appendix-d + thesis.md, LaTeX 01-introduction.tex, both `references.bib`, `references.md`). Surgical line-scoped edits (Ch2/5 untouched); mixed brackets kept academic keys. 72→54 used, 76→57 bib. Rebuild clean, 0 undefined, no `project_`/`Le, 2026` in PDF. Added "no self-repository citations" rule to the convention. |
| 2026-06-12 | Formatting | `frontmatter/cover.tex` | Blank page inserted after the cover page. | Resolved (Claude) | Removed the `\blankpage` between the two title pages per user request. Final blank page (end of document) retained per VNUK guideline. |
| 2026-06-12 | Formatting | `config/formatting.tex` | Paragraphs ran together (no visual separation), not academic. | Resolved (Claude) | Set `\parskip` to `0.6\baselineskip plus 2pt minus 1pt` while keeping the 1.25cm first-line indent; paragraphs are now clearly separated under 1.5 line spacing. |
| 2026-06-12 | Formatting | `frontmatter/cover.tex` | Signature block not aligned to the right margin (looked off-centre). | Resolved (Claude) | Replaced the fixed `tabular` with `tabularx{\textwidth}` using a right-aligned `X` column, so signature rules and names align to the right text margin (MIT-style). |
| 2026-06-12 | Formatting | `config/formatting.tex` | RQ4 final line ("improve coverage...") orphaned onto a new page. | Resolved (Claude) | Added `\clubpenalty`/`\widowpenalty`/`\displaywidowpenalty=10000` and `\raggedbottom`; RQ4 now stays intact on one page. |
| 2026-06-12 | Formatting | `main.tex` | Body font 11pt felt too small. | Resolved (Claude) | Raised document class to `12pt` per user (intentional deviation from the 11pt guideline). Calibri 12pt embedded; rebuilt with XeLaTeX. |
| 2026-06-12 | Front matter | `frontmatter/cover.tex` | Page 1: university name and year were not bold (inconsistent). | Resolved (Claude) | Bolded `\university` and the `\submissionplace, \submissionyear` line. |
| 2026-06-12 | Front matter | `frontmatter/cover.tex` | Page 2 (signature page) repeated the thesis title and author name already shown on page 1. | Resolved (Claude) | Removed the duplicated title and `by <author>` block; kept the degree statement and the signature block. |
| 2026-06-12 | Front matter | `frontmatter/approval.tex` | Approval wording did not match the VNUK template. | Resolved (Claude) | Rewrote to "This thesis ... has been approved in partial fulfillment of the requirements for the Degree of <degree>." plus a "Department of <program>" line, per template. |
| 2026-06-12 | TOC | `config/formatting.tex`, `main.tex` | TOC titled "Contents", no dot leaders, missing List of Figures/Tables entries. | Resolved (Claude) | Renamed to "Table of Contents" via `\AtBeginDocument` (babel was overriding it); added `tocloft` dotted leaders to chapter-level entries; added List of Figures / List of Tables to the TOC via `\addcontentsline`. |
| 2026-06-12 | Front matter | `frontmatter/cover.tex`, `frontmatter/author-contribution.tex`, `main.tex` | VNUK template alignment per user decisions. | Resolved (Claude) | (A) Added `© <year> <author>` copyright line to the title page. (B) Added the `A THESIS / Submitted in Partial Fulfillment ... Degree of <degree>` statement to the title page. (D) Removed the MIT-style signature page (template has only title + approval). (E) Renamed "Author Contribution" -> "Author Contribution Statement" and bibliography title "References" -> "Reference List". Rebuilt to 24 pages. |
| 2026-06-12 | T-093 | Chapter 1 citations / bibliography | Chapter 1 originally contained BibLaTeX citation groups while `docs/thesis-latex/bibliography/references.bib` was still the intentional T-096 placeholder. | Resolved (Claude) | Real bibliography was ported and self-repository citations were removed. Rebuild resolves Chapter 1 citations with 0 undefined keys. |
| 2026-06-12 | T-093 | Chapter 2 layout | Final Chapter 2 build reports one minor `Overfull \hbox (5.0471pt too wide)` in Section 2.1 around the verification/validation paragraph. | Closed (accepted) | Claude review: ~1.8 mm under the document's `\RaggedRight`; cosmetic and visually insignificant. Not fixed to avoid altering approved global line-breaking config (would risk shifting approved Chapter 1 / front-matter breaks). User approved Chapter 2 on 2026-06-12. |
| 2026-06-12 | T-093 | Chapter 3 figures (pre-conversion flag) | Chapter 3 is the first chapter with figures; the 11 diagram outputs exist as **SVG only** (`assets/diagrams/out/*.svg`), and XeLaTeX `\includegraphics` does not accept SVG (PDF/PNG/JPG only). The `svg` package is not loaded and `docs/thesis-latex/figures/` is empty except the logo. | Open | Resolve during Chapter 3 conversion: convert SVG→PDF into `figures/architecture|workflows/` (recommended), or load `\usepackage{svg}` + Inkscape on PATH. Copy the 2 PNG screenshots directly. If tooling is unavailable, insert `NEEDS_EVIDENCE` figure placeholders instead of breaking the build. |
| 2026-06-12 | T-093/T-100 | Table layout | Chapter 2 tables looked visually too tight because table rows had little breathing room. | Resolved (Codex) | Added global thesis table spacing in `docs/thesis-latex/config/formatting.tex`: `\arraystretch=1.22`, `\extrarowheight=1.5pt`, `\tabcolsep=5.5pt`, and modest `booktabs` rule spacing. Updated the LaTeX skill, architect agent, migration spec, formatting checklist, and document setup summary so later chapters follow the same table convention. Rebuilt PDF successfully. |
| 2026-06-12 | T-093 | Chapter 3 figures | Supersedes the pre-conversion Chapter 3 SVG blocker above. | Resolved (Codex) | Inkscape/rsvg/ImageMagick were unavailable, so the 11 Chapter 3 SVG diagrams were exported to PDF using Playwright/Chromium print-to-PDF and placed under `docs/thesis-latex/figures/architecture/` and `docs/thesis-latex/figures/workflows/`. The 2 PNG screenshots were copied to `docs/thesis-latex/figures/screenshots/`. Final build renders Figure 3.1 to Figure 3.13. |
| 2026-06-12 | T-093 | Chapter 3 / List of Figures | Final build reports a minor `Overfull \hbox (3.7059pt too wide)` in the List of Figures for the long Figure 3.13 caption. | Open (minor) | Non-blocking cosmetic warning; Chapter 3 body has no overfull `\hbox` after using breakable paths and local `\sloppy`/`\emergencystretch`. Leave for Claude visual review unless the user wants a LoF caption-shortening pass. |
| 2026-06-12 | T-093 | Chapter 3 figure readability | User feedback: the left-to-right (`flowchart LR`) Mermaid diagrams rendered very small/unreadable when fit to the portrait text column. Root cause (Claude): the LR PDFs had extreme aspect ratios (e.g. POM application workflow 1816×89 pt = 20:1; POM concept 1569×106 = 15:1; API pipeline 1308×115 = 11:1), so `\includegraphics[width=\textwidth]` shrank content to ~25-35% (height down to ~0.8 cm). | Resolved (Claude) | Re-authored 8 linear diagrams from `flowchart LR` to `flowchart TB` in `assets/diagrams/src/*.mmd` (POM concept, POM application workflow, fixture DI flow, shared-runtime context, API service pipeline, test-data/DTO cleanup, copilot agentic workflow, framework architecture) and re-rendered SVG masters (`assets/diagrams/out/`) plus tightly-cropped PDFs via `npx @mermaid-js/mermaid-cli ... --pdfFit` into `figures/architecture|workflows/`. Changed all 11 Chapter 3 `\includegraphics` to `[width=\textwidth,height=0.8\textheight,keepaspectratio]` so tall figures bind on height and wide ones on width — neither overflows. Rebuilt via `latexmk -xelatex`: PASS, 49 pages, 0 undefined, 0 overfull `\vbox`/`\hbox`. The 3 inherently-wide breadth diagrams (concept map 3.8, UI/API execution flows 3.9/3.10) remain full-width (~5 cm tall, acceptable); they are breadth-driven, not direction-driven, so TB would not help without restructuring approved content. |
| 2026-06-13 | T-093 | Chapter 3 content refinement | User-directed refinements during review: (1) Figures 3.4 (fixture DI flow) and 3.5 (shared-runtime context flow) were redundant; (2) the 3 wide breadth diagrams needed enlargement; (3) confirmation that the concept map should stay at §3.6 (not replace the §3.1 layered architecture). | Resolved (Claude) | (1) Removed Figure 3.5 (`shared-runtime-context-flow`) and re-pointed the §3.3.2 prose to Figure 3.4, whose caption already covers shared runtime context. Chapter 3 now has 12 figures (auto-renumbered 3.1-3.12). This is an intentional divergence from the frozen Markdown source (13 figures), documented in `conversion-register.md`. (2) Added `\usepackage{rotating}` and placed the concept map + both sequence diagrams on full landscape pages via `sidewaysfigure` with `\includegraphics[width=0.92\textheight,height=0.95\textwidth,keepaspectratio]` — they now use the page height as width and are much larger. (3) Kept both the layered architecture (§3.1 overview) and the concept map (§3.6 quality), confirmed complementary; concept-map nodes kept intact because they map 1:1 to Table 3.2. Rebuilt via `latexmk -xelatex`: PASS, 50 pages, 0 undefined, 0 overfull. |
| 2026-06-13 | T-093 | Chapter 3 refinement round 2 | User feedback after the first refinement round: (1) review/improve the Figure 3.1 layered-architecture diagram; (2) opinion on whether landscape figures are internationally acceptable; (3) revert the UI/API execution-flow figures from landscape back to portrait full-width; (4) reposition the 2 SUT screenshots that were stuck at the chapter end. | Resolved (Claude) | (1) Redesigned `framework-architecture.mmd` from a side-by-side row (inner `direction LR`) into a true vertical layered stack — test specification → abstraction (Page Objects/workflows \| API services) → framework core (fixtures \| utilities) → SUT, with data/config and reporting as cross-cutting; re-rendered SVG + cropped PDF (now 582x651 pt vs old 1128x360). (2) Landscape/`sidewaysfigure` confirmed standard and accepted by IEEE/ACM/Springer/Elsevier; retained for the breadth-driven concept map only (now Fig 3.9). (3) Reverted `ui-test-execution-flow` and `api-test-execution-flow` from `sidewaysfigure` to portrait `figure` at `[width=\textwidth,height=0.8\textheight,keepaspectratio]`. (4) Moved the 2 Unsplash SUT screenshots from the chapter end up to §3.1 beside their first reference (now Fig 3.2-3.3). Rebuilt via `latexmk -xelatex`: PASS, 49 pages, 0 undefined, only the two pre-existing cosmetic overfull `\hbox` (LoF caption + Chapter 2). |
| 2026-06-13 | T-093 | Figure 3.1 node typography | User: improve the in-node content of Figure 3.1 (the chapter's opening figure) — (1) clarify whether the concept name + repo path on the two lines were duplicated; (2) the "Browser, API, and element utilities / core, utils" node wrapped awkwardly; (3) emphasize the first (concept) line vs the second (path) line. | Resolved (Claude) | Confirmed the two lines are intentional (line 1 = conceptual layer name, line 2 = repository-folder evidence for traceability, consistent with Table 3.1) and kept both. Applied HTML labels across all 9 nodes: `<b>` for the concept name and `<i>` for the repo path, so the emphasis is clear. Shortened "Browser, API, and element utilities" to "Core utilities" and "Page Objects and workflows" to "Page Objects &amp; workflows" to balance node widths and remove the bad wrap. Re-rendered SVG (9 `<b>` + 9 `<i>` confirmed) + cropped PDF (516x615 pt). Rebuilt via `latexmk -xelatex`: PASS, 49 pages, 0 undefined, 12 LoF entries, only the two pre-existing cosmetic overfull `\hbox`. |
| 2026-06-13 | T-093 | Chapter 4 Figure 4.1 | First Chapter 4 build reported `Float too large for page by 179.58107pt` because the VS Code screenshot was constrained by width only. | Resolved (Codex) | Changed Figure 4.1 to `\includegraphics[width=0.92\textwidth,height=0.75\textheight,keepaspectratio]{...}`. Rebuilt via `scripts/build.ps1`: PASS, 57 pages, 0 undefined citations/references, no Chapter 4 float-too-large warning. |
| 2026-06-13 | T-093 | Chapter 4 diagrams | The handoff flagged 3 Chapter 4 Mermaid diagrams as `flowchart LR`, which would render too small in portrait LaTeX if left as wide left-to-right flows. | Resolved (Codex) | Re-authored `playwright-reporting-pipeline.mmd`, `copilot-script-generation-workflow.mmd`, and `copilot-code-review-workflow.mmd` from `flowchart LR` to `flowchart TB`; re-rendered SVG masters and cropped PDFs with Mermaid CLI `--pdfFit`; inserted PDFs with `[width=\textwidth,height=0.8\textheight,keepaspectratio]`. |
| 2026-06-13 | T-093 | Chapter 4 academic-quality review | Deep Claude review found the conversion faithful but flagged "AI-sounding" prose: a repetitive evaluation-caveat closing nearly every section in identical template form, no chapter lead-in, two self-referential "Chapter 4 focuses…" meta-sentences, "The thesis records/describes…" self-reference, and British/American spelling inconsistency. | Resolved (Claude) | Saved `reviews/ch4-latex-review-2026-06-13.md`; user chose to apply F1-F5. Edits to `04-implementation.tex` only: added a chapter lead-in carrying a single consolidated scope statement; removed the fully-redundant §4.6 caveat and reworded the §4.1/§4.3/§4.4 caveats for variation; trimmed the §4.2 and §4.3 "Chapter 4 focuses…" filler; changed "The thesis"→"This study" (§4.1, §4.5); standardized spelling (organisation→organization, sanitised→sanitized). Rebuilt via `latexmk -xelatex`: PASS, 56 pages, 0 undefined, no Chapter 4 body overfull. Intentional prose divergence from the frozen Markdown, documented. F6 (hard-coded "Figure 5.4"/"Appendix D") deferred to Chapter 5/appendix conversion. Awaiting user approval. |
| 2026-06-13 | T-093 | Chapter 5 conversion | Converted Chapter 5 to LaTeX and copied four PNG report/benchmark/trace figures. Initial sandboxed build hit a MiKTeX/Perl log-pipe access issue; escalated build completed. | Resolved (Codex) | `scripts/build.ps1` PASS with XeLaTeX+Biber, `build/main.pdf` 68 pages, 0 undefined citations/references. Added short optional captions for Chapter 5 figures to reduce new List of Figures overfull warnings. Remaining warnings are cosmetic/global: pre-existing LoF entries, accepted Chapter 2 box, Appendix D placeholder path overfull boxes, and MiKTeX update advisories. |
| 2026-06-13 | T-093 | Chapter 5 figure numbering | Chapter 5 Markdown names the HTML report as Figure 5.1 while the external benchmark figures are discussed before the execution-results section. Pure source-order conversion would have numbered the benchmark figures first. | Resolved (Codex) | Placed the HTML report figure definition in the evidence-basis portion of Chapter 5 so LaTeX numbering aligns with the Markdown figure register. Verified in `build/main.aux`: HTML report Figure 5.1, external benchmark Figures 5.2-5.3, and trace viewer Figure 5.4. Cross-references remain semantic through `\Cref{}`. |
| 2026-06-13 | T-093 | Chapter 6 conversion | Converted Chapter 6 from the approved Markdown source. The first sandboxed build repeated the known MiKTeX/Perl log-pipe access issue. | Resolved (Codex) | Re-ran `scripts/build.ps1` outside the sandbox: PASS with XeLaTeX+Biber, `build/main.pdf` rebuilt to 71 pages. Final log has 0 undefined citations/references. No Chapter 6 overfull boxes were reported; remaining overfull warnings are existing LoF/LoT and Chapter 5 table-caption/body items. Marker scan found no `project_`, raw `[@...]`, `NEEDS_`, `PLACEHOLDER`, `migrationpending`, or hard-coded `Figure~5.4`. |
| 2026-06-14 | T-094 | Front matter build | The first sandboxed build repeated the known MiKTeX/Perl log-pipe access issue even though the shell command returned exit code 0. | Resolved (Codex) | Re-ran `scripts/build.ps1` outside the sandbox: PASS with XeLaTeX+Biber, `build/main.pdf` rebuilt to 72 pages. Final log has 0 undefined citations/references. |
| 2026-06-14 | T-094 | `frontmatter/definitions.tex` | Initial `description` list conversion produced an `enumitem` negative labelwidth warning; replacing it with bold term paragraphs then produced one small overfull line around the JUnit Report definition. | Resolved (Codex) | Re-rendered Definitions as bold term paragraphs wrapped in a local `sloppypar`, preserving the approved Markdown wording and APA citation conversions. Rebuild PASS; no new front-matter overfull warning remains. Remaining warnings are pre-existing/global LoF/LoT and Chapter 5 items plus MiKTeX update advisories. |
| 2026-06-14 | T-095 | Appendices build | The first sandboxed build repeated the known MiKTeX/Perl log-pipe access issue even though the shell command returned exit code 0. | Resolved (Codex) | Re-ran `scripts/build.ps1` outside the sandbox: PASS with XeLaTeX+Biber, `build/main.pdf` rebuilt to 80 pages. Final log has 0 undefined citations/references. |
| 2026-06-14 | T-095 | Appendix tables and paths | Initial appendix conversion produced large overfull boxes from long source paths and one Appendix C table that was too tall for a page. | Resolved (Codex) | Replaced long paths with breakable `\path{}` where appropriate, split Appendix C into two smaller `tabularx` tables, and used smaller table font for dense artifact maps. Rebuild PASS; appendix-specific overfull warnings were removed. Remaining overfull warnings are pre-existing/global LoF/LoT/Chapter 5/bibliography cosmetics plus MiKTeX update advisories. |
| 2026-06-14 | TOC feedback | Table of Contents | User requested consistent uppercase top-level ToC entries and explicit `CHAPTER` before numbered chapter entries; follow-up clarified that numbered child sections such as 1.1, 2.1, and 3.x must remain title case. | Resolved (Codex) | Added `CHAPTER~` chapter-number prefix via `tocloft`, widened chapter number spacing, uppercased numbered chapter titles and manual top-level entries only, and preserved child section/subsection/appendix-child case. Rebuild PASS: `build/main.pdf` 78 pages, 0 undefined citations/references. |
| 2026-06-14 | T-097 | Figure migration | Final figure migration needed a reproducible Mermaid-to-PDF workflow and register alignment. A sandboxed `npx -y @mermaid-js/mermaid-cli` run failed because npm was forced into cache-only mode; sandboxed LaTeX build also repeated the known MiKTeX/Perl log-pipe issue. | Resolved (Codex) | Implemented `scripts/convert-svg-to-pdf.ps1` and ran it outside the sandbox to regenerate 13 active Mermaid diagrams as cropped PDF vector assets. Screenshots/report figures remain PNG. Updated `figure-register.md` to final LaTeX numbering and marked the redundant `shared-runtime-context-flow` detail diagram as excluded from the final LaTeX thesis. Rebuilt with `scripts/build.ps1` outside the sandbox: PASS, `build/main.pdf` 78 pages, 0 undefined citations/references; no `.svg` graphics are included by LaTeX. |
| 2026-06-14 | Front matter | Approval Page | User requested removal of `Thesis Co-Advisor: None` because the thesis is an individual project without a co-advisor. | Resolved (Codex) | Removed the co-advisor row from `docs/thesis-latex/frontmatter/approval.tex`, `docs/thesis-workspace/front-matter/approval-page.md`, and assembled `docs/thesis-workspace/thesis.md`. Rebuilt with `scripts/build.ps1` outside the sandbox: PASS, `build/main.pdf` 78 pages, 0 undefined citations/references. PDF text extraction confirms the Approval Page no longer contains `Thesis Co-Advisor` or `None`. |
